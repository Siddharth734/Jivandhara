# Architecture Decisions Index

This directory records decisions that future changes should preserve when they are clearly evidenced by the current code.

No historical ADRs were created during bootstrap because the repository does not document the original reasoning behind its architecture. The current implementation itself is documented in `docs/architecture/` and `docs/patterns/`.

Potential future ADRs should describe an observed, important architectural choice and clearly separate evidence from inferred rationale.
