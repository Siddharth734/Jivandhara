---
description: Run honest PROJECT J validation for a change
---

Determine the narrowest relevant validation. `code/test.py` is empty and no automated test suite exists. Use static checks or a manual launch from `code/` with `python main.py` when appropriate. Exercise the changed path and report exactly what ran, including environment limitations. Never claim an unrun test passed.
